# T9 - Listas Ligadas - Semana 06/04/26

## Parte 1 - 07/04/26

Sumário: Listas Ligadas

1. `linked_lists0.c` - introduz listas ligadas; define uma `struct` simples e as funções `criar_celula`, `inserir_inicio` e `imprimir_lista`. Alerta para o facto que devemos libertar a memória alocada (exemplo seguinte).

2. `linked_lists1.c` - este segundo ficheiro exemplo inclui o código do ficheiro anterior e introduz duas novas funções: `libertar_lista` e `comprimento_lista`.

3. `linked_lists2.c` - o terceiro exemplo desta sequência inclui o código do ficheiro anterior e introduz as funções `ultima_celula` e `inserir_fim`.



